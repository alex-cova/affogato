package dev.affogato.golden

class ThisCounter(var count: int) {
    increment(): int {
        this.count = this.count + 1
        return this.count
    }
}

class ThisExplicit {
    run(): int {
        let counter = ThisCounter(count = 1)
        return counter.increment()
    }
}
