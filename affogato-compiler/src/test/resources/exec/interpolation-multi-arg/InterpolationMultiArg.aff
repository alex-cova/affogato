package dev.affogato.exec

class InterpolationMultiArg {
    join(a: String, b: String): String {
        return a + b
    }

    static func run() {
        let a = InterpolationMultiArg()
        println("${a.join("a", "b")}")
    }
}
