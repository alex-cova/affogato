package dev.affogato.golden

class SwitchLocalExpression {
    describe(width: int): String {
        let label = switch width {
            case 0 -> "empty"
            case 1 -> "thin"
            default -> "filled"
        }
        return "width:" + label
    }
}
