package dev.affogato.golden

class IncrementDecrement {
    run(): int {
        var i = 0
        i++
        i++
        i--
        ++i
        --i
        return i
    }

    modulo(x: int): int {
        var r = x
        r %= 3
        return r
    }
}
