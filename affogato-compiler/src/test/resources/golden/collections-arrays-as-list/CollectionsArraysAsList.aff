package dev.affogato.golden

import java.util.Arrays

class CollectionsArraysAsList {
    run(): String {
        let values = Arrays.asList("a", "b")
        let first = values.get(0)
        let size = values.size()
        return first + ":" + size
    }
}
