package dev.affogato.golden

import dev.affogato.golden.interop.Role

@Role("reader")
@Role("writer")
class RepeatableAnnotatedType {
    label(): String {
        return "repeatable"
    }
}

class AnnotationsRepeatable {
    run(): String {
        return RepeatableAnnotatedType().label()
    }
}
