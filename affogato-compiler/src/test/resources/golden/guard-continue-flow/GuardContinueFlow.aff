package dev.affogato.golden

import java.util.List

class GuardContinueFlow {
    firstString(values: List<Object>): String {
        for value in values {
            guard value is String else {
                continue
            }
            return value as String
        }
        return "none"
    }
}
