package dev.affogato.golden

class AssignmentCompound {
    compute(value: int): int {
        var total = value
        total += 3
        total -= 1
        total *= 2
        total /= 4
        return total
    }
}
