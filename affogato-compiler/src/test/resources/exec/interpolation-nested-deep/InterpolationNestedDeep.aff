package dev.affogato.exec

class InterpolationNestedDeep {
    greet(s: String): String {
        return s
    }

    static func run() {
        let a = InterpolationNestedDeep()
        println("${a.greet("${a.greet("y")}")}")
    }
}
