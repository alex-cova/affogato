package dev.affogato.exec

class Counter(var n: int) { }

class PropertyCompoundAssign {
    static func run() {
        let c = Counter(10)
        c.n += 5
        c.n -= 2
        c.n *= 3
        println(c.n)
    }
}
